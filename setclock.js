//Author: Michael Neises
//Last Edited: Feb 8

function setclock(hours, minutes) {

    userHours = hours;
    userMinutes = minutes;

    if( isNaN(userHours) ) {
       
        userHours = 0;
    }
		
    if( isNaN(userMinutes) ) {
			
        userMinutes = 0;
    }		

	
}
